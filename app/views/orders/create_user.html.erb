<%= simple_form_for @u, :url => orders_create_order_path, html: { class: 'form-horizontal' } do |f| %>
  <%= f.input :email, label: 'Введите ваш email' %>
  <%= f.input :phone_number, label: 'Введите ваш номер телефона' %>
  <%= f.button :submit, 'Подтвердить', html: {class: "btn btn-primary"}%>
<% end %>